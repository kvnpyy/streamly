/** Max channels in the programme guide — full catalog freezes view switches. */
export const LIVE_GUIDE_MAX_CHANNELS = 72;

/** Max channels in the virtualized list view. */
export const LIVE_LIST_MAX_CHANNELS = 240;
